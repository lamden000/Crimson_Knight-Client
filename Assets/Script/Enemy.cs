using UnityEngine;

public class Enemy : MonoBehaviour
{
    private bool isDead;

    public bool IsDead { get { return isDead; } }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
